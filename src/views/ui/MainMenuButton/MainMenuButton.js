export default class MainMenuButton {
    constructor(menuOption, options) {
        this.menuOption = menuOption
        this.isSelected = false
        this.cursorPosition = options.cursorPosition
    }

    draw() {

    }
}